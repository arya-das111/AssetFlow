{
  "name": "assetflow-workspace",
  "version": "1.0.0",
  "description": "AssetFlow Enterprise Asset & Resource Management System Root Workspace",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:setup": "npm run db:setup --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
